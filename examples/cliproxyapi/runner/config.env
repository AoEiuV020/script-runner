# runner 固定读取这个 key=value 配置文件。
# 路径类 value 相对 /etc/runner 解析，除非写绝对路径。

APP_EXECUTABLE_NAME=CLIProxyAPI
APP_ARGS="--config /etc/app/config.yaml"
CHECK_SCRIPT=scripts/check.sh
DOWNLOAD_SCRIPT=scripts/download.sh
