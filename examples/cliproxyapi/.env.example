# 可选：GitHub API token，用于提高公开 release API 的 rate limit。
# 公开仓库 release 不需要私有 repo 权限；token 只用于认证身份、提高额度。
# fine-grained token 需要能读取 public repository metadata；classic token 可不勾选 repo scope。
GITHUB_TOKEN=
